import React from 'react'
import PlanCorrection from '../PlanCorrection/PlanCorrection';

export default function AmbulancePlanCorrection(props) {
    return (
        <PlanCorrection rootNodeId={17} />
    );
}