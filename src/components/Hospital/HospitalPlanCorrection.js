import React from 'react'
import PlanCorrection from '../PlanCorrection/PlanCorrection';

export default function HospitalPlanCorrection(props) {
    return (
        <PlanCorrection rootNodeId={1} />
    );
}